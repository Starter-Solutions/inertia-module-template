<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Route prefix
    |--------------------------------------------------------------------------
    |
    | All module routes are placed below this URL segment. It defaults to the
    | module slug. Use an empty string to remove the prefix entirely.
    |
    */
    'route_prefix' => '{{MODULE_SLUG}}',
];
